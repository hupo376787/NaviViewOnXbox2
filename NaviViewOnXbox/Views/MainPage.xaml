<Page
    x:Class="NaviViewOnXbox.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Button x:Name="btn" Content="Focus me" HorizontalAlignment="Center"
                    Background="Gray"
                    GotFocus="btn_GotFocus"
                    LosingFocus="btn_LosingFocus"
                    PreviewKeyDown="btn_PreviewKeyDown"/>

            <Button x:Name="btn2" Content="Focus"
                    Background="Gray"
                    GotFocus="btn_GotFocus"
                    LosingFocus="btn_LosingFocus"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom"
                    PreviewKeyDown="btn2_PreviewKeyDown"/>
        </Grid>
    </Grid>
</Page>
